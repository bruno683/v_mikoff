{% extends 'base.html.twig' %}

{% block title %}Qui Suis-Je ?{% endblock %}

{% block body %}
    <div class="container">
    <h1>Qui-suis je ?</h1>
    {% if pages %}
        {% for page in pages %}
            <h1>{{page.title}}</h1>
            <p>{{page.content|raw}}</p>
            
            <img src="{{asset('/upload/images/pages/' ~ page.image)}}" class="illustration"alt="image de l'article">
            <hr>
            
        {% endfor %}
        {% else %}
                <h1>pas de présentations disponibles</h1>
    {% endif %}
    </div>
{% endblock %}
